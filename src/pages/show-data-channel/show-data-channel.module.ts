import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ShowDataChannelPage } from './show-data-channel';

@NgModule({
  declarations: [
    ShowDataChannelPage,
  ],
  imports: [
    IonicPageModule.forChild(ShowDataChannelPage),
  ],
})
export class ShowDataChannelPageModule {}
