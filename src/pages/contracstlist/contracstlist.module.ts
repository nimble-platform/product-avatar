import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ContracstlistPage } from './contracstlist';

@NgModule({
  declarations: [
    ContracstlistPage,
  ],
  imports: [
    IonicPageModule.forChild(ContracstlistPage),
  ],
})
export class ContracstlistPageModule {}
